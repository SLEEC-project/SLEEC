// rule conflict experiment

def_start
	event E0
	event E1
	event E2
	event E3
	measure m0 : boolean
	measure m1 : boolean
	measure m2 : numeric
def_end

rule_start

	// raise warning E1 never happens, R16 is redundant
	// similar to a Java warning about if(true)
	R16 when E0 and m0 and not m0 then E1
	
rule_end